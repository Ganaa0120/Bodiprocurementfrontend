export type StatusCfg = { label: string; color: string; bg: string };
export type StatusAction = {
  status: string; label: string;
  color: string; bg: string; border: string;
};
export type DocItem = { url: string; label: string };